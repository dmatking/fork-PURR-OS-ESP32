# apps/system/msn/app.pcat

name        = "msn"
version     = "1.0.0"
tier        = "claw"
author      = "PURR OS"
description = "MSN (Mesh Social Network): buddy list + rooms over whichever mesh backend (Meshtastic or MeshCore) is active, no phone required."

idf_requires = "esp_common driver freertos"
