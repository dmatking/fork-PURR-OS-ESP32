#pragma once

// KittenUI — text-mode shell for small displays (128×64 OLED, SSD1306).
// Targets: Heltec WiFi LoRa 32 V3, LilyGo T-Deck, T-Deck Plus.
// Replaces the old "smol" shell.
void kitten_ui_start();
