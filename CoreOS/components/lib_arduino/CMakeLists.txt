# lib_arduino — empty stub post Arduino strip-out.
# Previously a passthrough to espressif/arduino-esp32; now unused.
# Kept so any remaining REQUIRES references don't cause cmake errors.
idf_component_register()
